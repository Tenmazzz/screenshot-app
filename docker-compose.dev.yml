services:
  vue:
    build:
      context: .
      dockerfile: docker/vue/Dockerfile
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    networks:
      - interne
 
  laravel:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: dev
    volumes:
      - ./backend:/var/www/html
    networks:
      - interne
    depends_on:
      - postgres
      - redis
      - minio
 
  laravel-worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: dev
    command: php artisan queue:work --queue=broadcast_queue
    volumes:
      - ./backend:/var/www/html
    networks:
      - interne
    depends_on:
      - redis
      - laravel
 
  worker:
    build:
      context: .
      dockerfile: docker/worker/Dockerfile
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./worker:/app
    networks:
      - interne
    depends_on:
      - redis
 
  nginx:
    image: nginx:alpine
    ports:
      - "4242:80"
    volumes:
      - ./backend:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - interne
    depends_on:
      - laravel
      - vue
 
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    networks:
      - interne
 
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: screenshot_app
      POSTGRES_USER: ${DB_USERNAME:-root}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-root}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - interne
 
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${AWS_ACCESS_KEY_ID:-minioadmin}
      MINIO_ROOT_PASSWORD: ${AWS_SECRET_ACCESS_KEY:-minioadmin}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - interne
 
  soketi:
    image: quay.io/soketi/soketi:1.6.1-16-alpine
    environment:
      SOKETI_DEFAULT_APP_ID: ${PUSHER_APP_ID:-app_id}
      SOKETI_DEFAULT_APP_KEY: ${PUSHER_APP_KEY:-app_key}
      SOKETI_DEFAULT_APP_SECRET: ${PUSHER_APP_SECRET:-app_secret}
      SOKETI_HOST: 0.0.0.0
    ports:
      - "6001:6001"
    networks:
      - interne
 
networks:
  interne:
    driver: bridge
 
volumes:
  pg_data:
  minio_data:
