stages:
  - build
  - deploy

build:
  stage: build
  tags:
    - cmd_runner
  script:
    - cd $CI_PROJECT_DIR
    - docker compose build

deploy:
  stage: deploy
  tags:
    - cmd_runner
  script:
    - cd $CI_PROJECT_DIR
    - docker compose down
    - docker compose up -d
  when: manual
  only:
    - main